%% Trapzoidal 

x = input('Enter x values: ');
y = input('Enter y values: ');
n = length(x);

h = (x(end) - x(1)) / (n-1);
s = 0;
for i = 2:n-1
    s = s + y(i);
end
result = (h/2) * (y(1) + 2*s + y(end));
disp('Result using Trapezoidal Rule ='), disp(result);
